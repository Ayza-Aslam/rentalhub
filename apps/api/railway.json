{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm install -g pnpm && pnpm install --ignore-scripts && npx prisma generate && pnpm build"
  },
  "deploy": {
    "startCommand": "node dist/index.js"
  }
}